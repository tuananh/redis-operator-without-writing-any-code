apiVersion: tuananh.net/v1alpha1
kind: Redis
metadata:
  name: my-redis-replication
spec:
  compositionSelector:
    matchLabels:
      provider: local
      type: dev
      kind: keyvalue
  parameters:
    architecture: replication
